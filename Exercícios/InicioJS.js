var=> cria variável
let => não deixa criar variável com o mesmo nome
const => cria variável constante

typeof=> diz o tipo da variável
Exemplo:
const variavel = 3;
console.log(typeof variavel);

Operador (-) transforma o numero em negativo
let numero = 100;

Função arrow
Exemplo:
const somar = (x=0, y=0) => {
  return x+y;
}
ou
const somar = (x=0, y=0) => x+y;